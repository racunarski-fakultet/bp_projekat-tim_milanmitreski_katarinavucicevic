package validator;

import gui.MainFrame;
import parser.Parser;
import parser.sql.SQLParser;
import start.AppCore;

public class Validator {

    Parser parser = MainFrame.getInstance().getAppCore().getParser();


}
