package parser.sql;

public enum Tokens {
    SELECT,
    FROM,
    AGGREGATE,
    TABLECOLUMN,
    TABLE,
    JOIN,
    ON,
    ON_CONDITION,
    EQUALS,
    USING,
    USING_CONDITION,
    WHERE,
    GROUP,
    BY,
    BETWEEN,
    AND,
    IN,
    NOT,
    LIKE,
    NUMBER,
    STRING,
    LIST,
    GREATER,
    GREATER_EQUAL,
    LESS,
    LESS_EQUAL,
    OR,
    HAVING,
    ORDER,
    ASC,
    DESC;
}
